---
name: PlanAndApply

on:
  workflow_call:
    inputs:
      list_of_items:
        required: true
        type: string
jobs:
  demo:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cluster: ${{ fromJSON( inputs.list_of_items ) }}
        # cluster: ["x", "y"]
    steps:
      - name: demo
        run: |
          echo "Testing"
